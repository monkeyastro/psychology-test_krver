# Go/No-Go Korean local build - pinned environment
# Target: Windows x64 / Python 3.10.11 (64-bit)
#
# Install order:
#   py -3.10 -m venv .venv
#   .venv\Scripts\activate
#   python -m pip install "pip==24.0" "setuptools==66.1.1" wheel
#   pip install -r requirements-lock.txt
#   pip install --no-deps psychopy==2023.2.3
#
# psychopy must be installed LAST with --no-deps, because 2023.2.3
# declares pypi-search which no longer exists on PyPI.
#
# ASCII only on purpose: pip reads requirements files with the Windows
# locale codec (cp949 on Korean Windows) unless the file has a UTF-8 BOM.

# ---- DO NOT CHANGE THESE THREE ----
# pyglet 1.4.11  : pinned by PsychoPy on Windows. Window creation / flip timing.
# psychtoolbox   : RT measurement backend. 3.0.20+ is incompatible.
# numpy 1.26.4   : numpy 2.x removes np.alltrue -> PsychoPy crashes on Window().
pyglet==1.4.11
psychtoolbox==3.0.19.14
numpy==1.26.4

# ---- core runtime ----
scipy==1.10.1
pandas==2.0.3
matplotlib==3.7.5
pillow==10.4.0
openpyxl==3.1.5
questplus==2023.1
xmlschema==2.5.1
requests==2.32.3
future==1.0.0
packaging==24.1

# ---- TextBox2 text rendering (needed for Korean glyphs) ----
freetype-py==2.4.0
python-bidi==0.4.2
arabic-reshaper==3.0.0

# ---- participant info dialog ----
PyQt6==6.6.1
PyQt6-Qt6==6.6.1
PyQt6-sip==13.6.0

# ---- audio stack (loaded on psychopy import; experiment uses no sound) ----
soundfile==0.12.1

# ---- Windows platform ----
pypiwin32==223
pywin32==306

# ---- iohub stack: REQUIRED, do not remove ----
# gng.psyexp sets keyboardBackend='ioHub'. Keyboard._backend is a class
# attribute, so key_resp inherits iohub. Removing this changes the
# RT measurement instrument.
gevent==23.9.1
psutil==5.9.8
pyzmq==25.1.2
ujson==5.9.0
msgpack==1.0.7
msgpack-numpy==0.4.8
pyyaml==6.0.1
tables==3.8.0
pywinhook==1.6.2
